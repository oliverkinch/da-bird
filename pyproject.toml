[project]
name = "da-bird"
version = "0.1.0"
description = "First-draft utility for downloading BIRD benchmark datasets"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "harbor>=0.7.0",
    "huggingface-hub>=1.14.0",
    "openai>=1.0.0",
    "requests>=2.34.2",
]
